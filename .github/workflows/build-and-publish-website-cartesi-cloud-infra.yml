name: build-and-publish-website-cartesi
run-name: Building and Publishing Rives barebones with Doom website
on: [workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    environment: "Cartesi Cloud Infra"
    defaults:
      run:
        working-directory: doom-arena-website
    steps:
      - uses: actions/checkout@v6

      - name: Set up config
        env:
          ENV_VARIABLES: >-
            EMULATOR_URL
            CARTRIDGES_URL
            CHAIN_ID
            APPLICATION_ADDRESS
            NODE_URL
            INPUT_BOX_ADDRESS
          EMULATOR_URL: ${{ vars.EMULATOR_URL }}
          CARTRIDGES_URL: ${{ vars.CARTRIDGES_URL }}
          CHAIN_ID: ${{ vars.CHAIN_ID }}
          APPLICATION_ADDRESS: ${{ vars.APPLICATION_ADDRESS }}
          NODE_URL: ${{ vars.NODE_URL }}
          INPUT_BOX_ADDRESS: ${{ vars.INPUT_BOX_ADDRESS }}
        run: |
          rm src/config.js
          for var in $ENV_VARIABLES; do
            echo "export const $var = '${!var}'" >> src/config.js
          done
          echo Final src/config.js:
          cat src/config.js

      - uses: actions/setup-node@v6
      - name: Install Dependencies
        run: npm install

      - name: Build
        run: npm run build
        env:
          CI: true # This is done to prevent output being too verbose.

      - name: Upload static files as artifact
        id: deployment
        uses: actions/upload-pages-artifact@v3 # or specific "vX.X.X" version tag for this action
        with:
          path: doom-arena-website/dist/
          retention-days: 2

  # Deploy job
  deploy:
    if: startsWith(github.ref, 'refs/heads/main')
    needs: build
    permissions:
      pages: write # to deploy to Pages
      id-token: write # to verify the deployment originates from an appropriate source
    defaults:
      run:
        working-directory: doom-arena-website
    # Deploy to the github-pages environment
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
